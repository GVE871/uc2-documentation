[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "uc2-documentation"
authors = [{name = "Giovanni Viciconte", email = "giovanni.viciconte@dcs-computing.com"}]
dynamic = ["version", "description"]
